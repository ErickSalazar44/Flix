
export default function TrailerModal({
    params,
}: {
    params: { actor: string};
}) {
    const id = params;

    console.log(id)
    console.log('holaa ahora si ')


    return (
        <div className="bg-white text-black text-2xl min-h-screen w-full flex items-center justify-center relative z-[100]">
            <span >Close modal</span>
            FUNCIONOOO CTMRRR
        </div>
    )
}